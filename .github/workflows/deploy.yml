name: Deploy Team Login

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Create .env with API_KEY
        run: echo "API_KEY=${{ secrets.API_KEY }}" > .env

      - name: Build / Deploy
        run: |
          # agar tu static HTML use kar raha hai:
          echo "Deploy commands here"
